{
	"name": "nodeAuthServer",
	"version": "1.0.0",
	"description": "node server for authentication with passport",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "nodemon -L ./src/lib/index.js --exec babel-node --presets es2015,stage-0",
		"build": "babel-node src/lib -d dist --presets es2015,stage-0"
	},
	"author": "Tan Guven",
	"license": "MIT",
	"dependencies": {
		"bcrypt-nodejs": "0.0.3",
		"body-parser": "^1.15.2",
		"express": "^4.14.0",
		"jwt-simple": "^0.5.0",
		"mongoose": "^4.6.0",
		"morgan": "^1.7.0",
		"nodemon": "^1.10.2",
		"passport": "^0.3.2",
		"passport-jwt": "^2.1.0",
		"passport-local": "^1.0.0"
	},
	"devDependencies": {
		"babel-cli": "^6.14.0",
		"babel-core": "^6.14.0",
		"babel-preset-es2015": "^6.14.0",
		"babel-preset-stage-0": "^6.5.0",
		"babel-watch": "^2.0.2"
	}
}
